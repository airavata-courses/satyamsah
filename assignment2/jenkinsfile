node {
   stage('Preparation') { // for display purposes
      // Get some code from a GitHub repository
      checkout(
          [$class: 'GitSCM', 
          branches: [[name: 'v2_newdockerized']], 
          doGenerateSubmoduleConfigurations: false, 
          extensions: [], 
          submoduleCfg: [], 
          userRemoteConfigs: [[url: 'https://github.com/airavata-courses/satyamsah.git']]
          ]
        )
   }
   
   stage('Clean'){
       sshagent (credentials: ['PK']) {
            sh 'ssh -o StrictHostKeyChecking=no -l satyam1 <remote-ip> rm -rf /home/satyam1/assignment2'
            sh 'scp -r "/var/lib/jenkins/workspace/test/assignment2" satyam1@<remote-ip>:/home/satyam1/assignment2'
        }
    }
       
  
   stage('Build') {
      sshagent (credentials: ['PK']) {
            sh 'ssh -o StrictHostKeyChecking=no -l satyam1 <remote-ip> sudo bash /home/satyam1/assignment2/build.sh'
    }
   }
}
